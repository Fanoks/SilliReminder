Put your icons here:

- assets/icon.ico  (Windows executable/tray icon resource)
- assets/icon.png  (Window/taskbar icon for egui; PNG recommended e.g. 64x64)

Both will be embedded at build time via build.rs.
